import React from 'react';
import PhotoContainer from './PhotoContainer';

const SearchResult = ({computedMatch}) => {
  
  return(
    <h2>Eventually there will be results here, how cool is that</h2>
  )
}

export default SearchResult;